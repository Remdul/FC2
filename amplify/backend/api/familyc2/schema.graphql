type UserPool @model @auth(rules: [{allow: public}]) {
  id: ID!
  userName: String
  createdDate: String!
  firstLogin: Boolean
  userEmail: String
  userSub: String
  firstName: String
}

type Note @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  points: Int!
  description: String
}
 